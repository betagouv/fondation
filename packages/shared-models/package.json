{
  "name": "shared-models",
  "private": true,
  "version": "1.0.0",
  "description": "Shared models between the API and the Web App",
  "main": "./dist/models/index.js",
  "types": "./models/index.ts",
  "scripts": {
    "build": "rimraf dist/ && tsc",
    "build:watch": "tsc --watch",
    "dev": "pnpm build:watch"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.20",
    "form-data": "^4.0.4",
    "rimraf": "^6.1.0",
    "type-fest": "^4.41.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}