name: Migrate Db
permissions:
  contents: read

on:
  workflow_call:
    secrets:
      SCALINGO_API_TOKEN:
        required: false
      SCALINGO_SSH_KEY:
        required: false

    inputs:
      environment:
        type: string
        required: true
      tags_current:
        type: string
        required: true
      tags_previous:
        type: string
        required: false

jobs:
  migrate:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
          fetch-depth: 200
          fetch-tags: true

      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093
        with:
          version: 11.11.0
          run_install: false

      - name: Use Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: '24.12.0'
          cache: 'pnpm'

      - name: Install scalingo CLI
        env:
          token: ${{ secrets.SCALINGO_API_TOKEN }}
        run: |
          curl -O https://cli-dl.scalingo.com/install && bash install
          echo "$HOME/bin" >> $GITHUB_PATH
          scalingo login --api-token ${token}

      - name: Run migration
        shell: bash
        env:
          SCALINGO_APP_REGION: ${{ vars.SCALINGO_APP_REGION }}
          SCALINGO_API_APP_NAME: ${{ vars.SCALINGO_API_APP_NAME }}
          SCALINGO_SSH_KEY: ${{ secrets.SCALINGO_SSH_KEY }}
          SCALINGO_API_TOKEN: ${{ secrets.SCALINGO_API_TOKEN }}
        run: |
          ./apps/api/scripts/migrate.sh
